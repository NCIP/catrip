CREATE TABLE THREE_DIMENSIONAL_SIZES ( 
	ID NUMERIC(10,2) NOT NULL,
	GREATEST_DIMENSION INTEGER,
	ADDITIONAL_DIMENSION_Y INTEGER,
	ADDITIONAL_DIMENSION_Z INTEGER,
	MVR VARCHAR(255),
	PRIMARY KEY(ID)
) 
;

ALTER TABLE THREE_DIMENSIONAL_SIZES ADD CONSTRAINT FK_THREE_DIMENSIONAL_SIZES 
FOREIGN KEY (ID) REFERENCES ANNOTATION_SET (ID)
;






