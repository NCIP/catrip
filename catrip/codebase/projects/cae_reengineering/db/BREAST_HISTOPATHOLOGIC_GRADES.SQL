CREATE TABLE BREAST_NOTT_HISTOPATH_GRADES
(
  ID                    NUMBER(10,2)            NOT NULL,
  TUBULE_FORMATION      INTEGER,
  NUCLEAR_PLEOMORPHISM  INTEGER,
  MITOTIC_COUNT_N       INTEGER,
  TOTAL_SCORE           INTEGER,
  TOTAL_SCORE_MVR       VARCHAR2(255),
  PRIMARY KEY(ID)
);

ALTER TABLE BREAST_NOTT_HISTOPATH_GRADES ADD CONSTRAINT FK_BREAST_NOTT_HGRADES 
FOREIGN KEY (ID) REFERENCES ANNOTATION_SET (ID);

CREATE TABLE BREAST_OTHER_HISTOPATH_GRADES
(
  ID                    NUMBER(10,2)            NOT NULL,
  SYSTEM_NAME           VARCHAR2(255),
  SCORE                 INTEGER,
  SCORE_MVR             VARCHAR2(255),
  MITOTIC_COUNT_O       INTEGER,
  PRIMARY KEY(ID)
);

ALTER TABLE BREAST_OTHER_HISTOPATH_GRADES ADD CONSTRAINT FK_BREAST_OTH_HGRADES 
FOREIGN KEY (ID) REFERENCES ANNOTATION_SET (ID);

/**
CREATE TABLE BREAST_HISTOPATHOLOGIC_GRADES ( 
	ID NUMERIC(10,2) NOT NULL,
	TUBULE_FORMATION INTEGER,
	NUCLEAR_PLEOMORPHISM INTEGER,
	MITOTIC_COUNT_N INTEGER,
	TOTAL_SCORE INTEGER,
	TOTAL_SCORE_MVR VARCHAR(255),
	DISCRIMINATOR VARCHAR(4),
	SYSTEM_NAME VARCHAR(255),
	SCORE INTEGER,
	SCORE_MVR VARCHAR(255),
	MITOTIC_COUNT_O INTEGER,
	PRIMARY KEY(ID)
) 
;

ALTER TABLE BREAST_HISTOPATHOLOGIC_GRADES ADD CONSTRAINT FK_BREAST_HIST_GRADES 
FOREIGN KEY (ID) REFERENCES ANNOTATION_SET (ID)
;
**/









