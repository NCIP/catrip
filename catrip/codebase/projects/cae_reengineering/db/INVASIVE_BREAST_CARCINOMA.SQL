CREATE TABLE INVASIVE_BREAST_CARCINOMA ( 
	ID NUMERIC(10,2) NOT NULL,
	VENOUS_LYMPHATIC_INVASION VARCHAR(255),
	LOCATION_MVR VARCHAR(255),
	PRIMARY KEY(ID)
) 
;

ALTER TABLE INVASIVE_BREAST_CARCINOMA ADD CONSTRAINT FK_INVASIVE_BREAST_CARCINOMA 
FOREIGN KEY (ID) REFERENCES ANNOTATION_SET (ID)
;



